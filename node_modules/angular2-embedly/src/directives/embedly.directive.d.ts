import { ElementRef, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { EmbedlyService } from '../services';
export declare class EmbedlyDirective implements OnChanges {
    private el;
    private embedly;
    url: string;
    width: number;
    onEmpty: EventEmitter<Object>;
    constructor(el: ElementRef, embedly: EmbedlyService);
    ngOnChanges(changes: SimpleChanges): any;
}
